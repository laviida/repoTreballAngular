import { ErrorStarDirective } from './error-star.directive';

describe('ErrorStarDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorStarDirective();
    expect(directive).toBeTruthy();
  });
});
